
const express = require('express');
const router = express.Router();

// This function will be called when the user searches for a recipe
router.get('/search', (req, res) => {
    
});

module.exports = router;